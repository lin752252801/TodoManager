node_modules/
dist/

# 程序运行时写在 exe 旁边的用户数据，不进仓库
data/
config/

# 调试脚本与临时截图
_*.js
_*.png
_*.json
*.log

# 搜狗输入法 / .NET 会把配置写进子进程的工作目录，文件名叫 *user.config。
# 2026-09-25 因为测试子进程把 cwd 设成了项目根，根里出现过 8 个（已清理）。
# 留这条做保险：万一又冒出来，不至于当成未跟踪文件混进仓库。
*user.config
